/*
Trees are a data structure that have what we call a hierarchical structure as opposed
to something like link lists or arrays which are linear. Tree usually starts with a single root
node or a parent node, and every child of the tree descends from this root node.

-DOM
-Facebook comments
-Abstract syntax tree
-File systems in computer
 */